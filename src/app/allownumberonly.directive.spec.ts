import { AllownumberonlyDirective } from './allownumberonly.directive';

describe('AllownumberonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AllownumberonlyDirective();
    expect(directive).toBeTruthy();
  });
});
